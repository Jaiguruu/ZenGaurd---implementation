# =============================================================================
# File: dashboard/requirements.txt
# Project: ZenGuard Zero Trust SIEM — Phase 6 Dashboard Dependencies
#
# Install: pip install -r dashboard/requirements.txt
# =============================================================================

# Flask — lightweight WSGI web framework for the dashboard server
flask>=3.0.0,<4.0.0

# Flask-CORS — Cross-Origin Resource Sharing headers for the REST API.
# Required when the frontend and API are served on different ports during dev.
flask-cors>=4.0.0

# requests — used by siem_listener.py to POST payloads to /api/ingest.
# Already in the root requirements.txt; listed here as an explicit reminder
# that this package must be present in the same venv as siem_listener.py.
requests>=2.31.0

# gunicorn — production WSGI server. Replace `python app.py` with:
#   gunicorn -w 4 -b 0.0.0.0:5000 app:app
# Not needed for local development runs.
gunicorn>=21.2.0
